{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "start-dev": "tsc-watch --project . --outDir ./build --onSuccess \"nodemon ./build/index.js\"",
    "bstart": "npm run build && npm run start",
    "build": "rimraf ./build && tsc",
    "start": "node build/index.js",
    "clean": "rm -rf build"
  },
  "author": "Debankle",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/morgan": "^1.9.2",
    "nodemon": "^2.0.7",
    "tsc-watch": "^4.2.9",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "chalk": "^3.0.0",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "rimraf": "^3.0.2"
  }
}
